# Everything Project Control needs. Nothing here requires a compiler,
# and the database (SQLite) is part of Python itself.
Flask>=3.0,<4.0
waitress>=3.0,<4.0

# Reading and writing the setup workbook (Setup -> Export / Import to Excel).
openpyxl>=3.1,<4.0

# Carmen, the assistant. Anthropic's own client rather than hand-rolled HTTP:
# typed errors worth catching apart, retries and timeouts already thought about,
# and a tool-use surface that will not drift out from under us. Pure Python —
# nothing here needs a compiler either.
anthropic>=1.4,<2.0

# The issued minutes as a real PDF, and the attachments stapled onto the end of
# it. reportlab draws the document; pypdf joins it to whatever is attached.
# Both are pure Python wheels — nothing here needs a compiler either.
reportlab>=4.0,<6.0
pypdf>=4.0,<7.0

# Triton, the quay element designer mounted at /triton (the triton folder). It is
# a FastAPI application, run inside this WSGI site through a2wsgi; pandas and
# numpy read the Plaxis workbooks, pyxlsb the binary .xlsb ones, python-docx
# writes the Word reports and Pillow draws their figures. All are wheels on
# PythonAnywhere — nothing to compile. Kept in step with Triton's pyproject.toml.
a2wsgi>=1.10
fastapi>=0.110
numpy>=1.26
pandas>=2.1
pillow>=10.1
python-docx>=1.1
python-multipart>=0.0.9
pyxlsb>=1.0.10
